---
import { getEntry } from 'astro:content'
import TallerListLayout from '../../../layouts/TallerListLayout.astro'

const page = await getEntry('pages', 'es/tallers')
if (!page) {
  throw new Error('pages/es/tallers not found')
}
---

<TallerListLayout frontmatter={page.data} />
